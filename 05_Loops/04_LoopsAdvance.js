const myNums =[1,2,3,4,5,6,7,8,9]

// filter in array in js 

const newNums = myNums.filter( (num) => num > 4)
console.log(newNums);

// if we are using {} scope we must write the return statement
// const newNums1 = myNums.filter( (num) => {
//     return num > 4})
// console.log(newNums);

